<?php
$title = 'My orders';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<div class="col-md-9">
    <table id="order-grid" class="table table-condensed table-hover table-striped" data-row-select="true"
           data-keep-selection="true">
        <thead>
        <tr>
            <th data-column-id="dateofsale">dateofsale</th>
            <th data-column-id="pricelistnum" data-order="desc">pricelistnum</th>
            <th data-column-id="payamount" data-order="desc">payamount</th>
            <th data-column-id="paytype" data-order="desc">paytype</th>
            <th data-column-id="cashlocation" data-order="desc">cashlocation</th>
            <th data-column-id="cityofsale" data-order="desc">cityofsale</th>
            <th data-column-id="pointofsale" data-order="desc">pointofsale</th>
            <th data-column-id="seller" data-order="desc">seller</th>
        </tr>
        </thead>

    </table>
    <!--/div-->
</div>

<script>
    $(function () {
        function init() {
            $("#order-grid").bootgrid({
                ajax: true,
                post: function () {
                    /* To accumulate custom parameter with the request object */
                    return {
                        id: "b0df282a-0d67-40e5-8558-c9e93b7befed"
                    };
                },
                url: "/restorder"
            });
        }

        init();
    })

</script>